<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.example.employee_system.infrastructure.mapper.EmployeeSkillMapper">
    
    
    	<select id="findByEmployeeId"
    			resultType="com.example.employee_system.presentation.response.EmployeeSkillSummaryResponse">
    			
    		SELECT
    			es.employee_skill_id AS employeeSkillId,
    			s.category_code AS categoryCode,
    			s.skill_name AS skillName,
    			es.experience_months AS experienceMonths,
    			es.skill_level AS skillLevel,
    			es.last_used_ym AS lastUsedYm,
    			es.remarks AS remarks
    			
    		FROM employee_skills es
    		INNER JOIN skills s
    			ON s.skill_id = es.skill_id
    			AND s.deleted_at IS NULL
    		WHERE es.employee_id = #{employeeId}
    			AND es.deleted_at IS NULL
    		ORDER BY
    			s.category_code ASC,
    			s.skill_name ASC
    		
    	
    	</select>
    	
     	<select id="countByEmployeeIdAndSkillId"
     			resultType="long">
     			
     		SELECT
		        COUNT(*)
		    FROM employee_skills
		    WHERE employee_id = #{employeeId}
		      AND skill_id = #{skillId}
		      AND deleted_at IS NULL
    	</select>
    	
    	<select id="findByEmployeeSkillId"
    			resultType="com.example.employee_system.presentation.request.EmployeeSkillRequest">
    				
    				SELECT
    					skill_id AS skillId,
    					experience_months AS experienceMonths,
    					skill_level AS skillLevel,
    					last_used_ym AS lastUsedYm,
    					remarks AS remarks
    				FROM employee_skills
    				WHERE employee_skill_id = #{employeeSkillId}
    					AND employee_id = #{employeeId}
    					AND deleted_at IS NULL
    			</select>
    	
    	<insert id="insert">

		    INSERT INTO employee_skills (
		        employee_id,
		        skill_id,
		        experience_months,
		        skill_level,
		        last_used_ym,
		        remarks,
		        created_at,
		        created_by,
		        updated_at,
		        updated_by
		    ) VALUES (
		        #{employeeId},
		        #{request.skillId},
		        #{request.experienceMonths},
		        #{request.skillLevel},
		        NULLIF(
		            #{request.lastUsedYm},
		            ''
		        ),
		        NULLIF(
		            #{request.remarks},
		            ''
		        ),
		        CURRENT_TIMESTAMP,
		        #{createdBy},
		        CURRENT_TIMESTAMP,
		        #{createdBy}
		    )
		
		</insert>
		
		<update id="updateByEmployeeSkillId">

            UPDATE employee_skills
			    SET
			        skill_id = #{request.skillId},
			        experience_months = #{request.experienceMonths},
			        skill_level = #{request.skillLevel},
			        last_used_ym = NULLIF(
			            #{request.lastUsedYm},
			            ''
			        ),
			        remarks = NULLIF(
			            #{request.remarks},
			            ''
			        ),
			        updated_at = CURRENT_TIMESTAMP,
			        updated_by = #{updatedBy}
			    WHERE employee_skill_id = #{employeeSkillId}
			      AND employee_id = #{employeeId}
			      AND deleted_at IS NULL
		</update>    
    
    </mapper>