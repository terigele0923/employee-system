<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper

	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.example.employee_system.infrastructure.mapper.ProjectMapper">
	
	<select id="countOpenProjects"
		resultType="long">
		
		SELECT
			COUNT(*)
		FROM projects p
		WHERE p.project_status = '01'
			AND p.deleted_at IS NULL
		
		<if test="salesUserId != null">
			AND p.sales_user_id = #{salesUserId}
		</if>
	</select>
	
	<select id="findProjectOptions"
        resultType="com.example.employee_system.presentation.response.ProjectOptionResponse">

    SELECT
        p.project_id AS projectId,
        p.project_no AS projectNo,
        p.project_name AS projectName,
        c.client_name AS clientName
    FROM projects p
    INNER JOIN clients c
        ON p.client_id = c.client_id
       AND c.deleted_at IS NULL
    WHERE p.deleted_at IS NULL
    ORDER BY
        p.project_no ASC,
        p.project_name ASC

</select>
       
</mapper>